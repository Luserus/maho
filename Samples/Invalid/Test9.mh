namespace Chaos;

public class Foo<T
{
    public Collections.Dictionary<int, char*> Dict = new Collections.Dictionary<int, char*>();

    public int Build(int seed, )
    {
        Foo<int> first = new Foo<int>(;
        Foo<int> second = put Foo<int>(test, );
        int[] values = new int[3]{1, , 3
        var collection = (int[])[1, 2, 3, 4
        ;return seed;  var s ="load of test string to see how the pointer works in error reporting."
    }
}
